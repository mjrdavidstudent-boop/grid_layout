# Layout Grid — Vite + React

A simple React app using CSS Grid to recreate the provided layout screenshot.

## Run (Windows PowerShell)

```powershell
npm install
npm run dev
```

Build for production:

```powershell
npm run build
npm run preview
```

Project structure:

- `src/App.jsx`: Grid layout markup
- `src/styles.css`: Grid + styling
- `vite.config.js`: Vite + React plugin
- `index.html`: App entry
